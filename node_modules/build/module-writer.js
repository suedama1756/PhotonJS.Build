var _system = require('system'),
    WrapperGenerator = require('./module-wrapper-generator.js');


function ModuleWriter() {

}

_system.extend(
    ModuleWriter.prototype,
    /**
     * @lends ModuleWriter.prototype
     */
    {
        generate : function(module, options) {
            var wrapperGenerator = new WrapperGenerator();
            var wrapper = wrapperGenerator.generate(module, options);


            var files = module.files;
            if (files) {
                files.forEach(function(file) {

                });
            }
        }

    });


module.exports = ModuleWriter;